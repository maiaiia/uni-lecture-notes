Have a table with multiple versions
Create multiple procedures 
moving from one procedure to the next can be done via calling procedures

Composite key: primary key composed of 2 columns. It can be turned into a normal form by splitting it into 2 tables, each having one primary key as one of the 2 keys in the composite

ACID 
