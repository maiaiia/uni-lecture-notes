# MatLab Cheat Sheet
___
Class: [[Probability & Statistics]]
Type: Cheat Sheet
Date: October 7th, 2025
___

`clc` - clear command panel (but not memory)
`clear all` - clear memory
`mean` - arithmetical average 

Coin Toss: `mean(rand(1,N)<0.5)`
`rand` - random number between 0 and 1
`randi(imax)`: random integer between 1 and `imax`
`randperm(n)`: random permutation of size `n`

`sum` - column-wise sum

`binoplf`